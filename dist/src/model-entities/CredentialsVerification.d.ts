import { ICredentialsverification } from '../model-interfaces';
import { LiveConnectionConstruct } from '../entities';
import { Model } from '../model-structures';
export declare class CredentialsVerification extends Model<ICredentialsverification> {
    constructor(connector?: LiveConnectionConstruct);
}
